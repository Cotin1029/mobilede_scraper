# Mobile.de スクレイパー - 失敗モード分析と問題点

## 現在のコードの問題点

### 1. レートリミット検出の問題

#### ❌ 問題1: HTTPステータスコードのみの検出
**場所**: `mobilede_main.js:347-355`, `mobilede_extractor.js` (検出なし)

```javascript
async function isAccessDenied(response) {
  if (!response) return false;
  try {
    const status = response.status();
    return [403, 429, 503, 401].includes(status);
  } catch {
    return false;
  }
}
```

**問題点**:
- **429 (Too Many Requests) のみ検出** → 実際のレートリミットは別の形式で返される可能性
- **HTMLコンテンツの検証なし** → 200 OKでも「Access Denied」ページが返される可能性
- **レスポンスヘッダー未確認** → `Retry-After`, `X-RateLimit-*` などの情報を無視
- **JavaScript実行後の検証なし** → クライアントサイドでブロックされる可能性

#### ❌ 問題2: レートリミット発生時の詳細ログ不足
- **どのURLで発生したか** → 記録されている
- **何回目のリクエストで発生したか** → 記録されていない
- **レスポンスボディの内容** → 記録されていない
- **レスポンスヘッダー** → 記録されていない
- **タイムスタンプ** → 失敗URLにはあるが、詳細なタイムラインなし

#### ❌ 問題3: レートリミット再現機能なし
- **意図的にレートリミットを発生させる機能がない**
- **リクエスト頻度を調整する機能が限定的**（固定のsleep時間のみ）

---

### 2. プロキシ管理の問題

#### ❌ 問題4: プロキシローテーション機能なし
**場所**: `mobilede_main.js:13`, `mobilede_extractor.js:17`

```javascript
const SCRAPERAPI_PROXY = `http://scraperapi:${SCRAPERAPI_KEY}@proxy.scraperapi.com:8002`;
```

**問題点**:
- **固定プロキシのみ** → ScraperAPIの固定エンドポイントを使用
- **プロキシ切り替え機能なし** → レートリミット発生時に別プロキシに切り替えられない
- **プロキシプール管理なし** → `proxies.json` が存在するが使用されていない

#### ❌ 問題5: プロキシ変更時のブラウザ状態リセット不十分
**場所**: `mobilede_main.js:285-302` (`safeCloseContext`)

```javascript
async function safeCloseContext() {
  // ...
  await currentContext.close();
  currentContext = null;
  consecutiveEmpty = 0;
}
```

**問題点**:
- **userDataDirの削除は行われる**（`getOrCreateContext`で毎回新規作成）
- **しかし、プロキシ変更時に明示的なクリア処理がない**
- **Cookie/Storageの明示的なクリアなし** → ブラウザ状態が残る可能性

---

### 3. エラー処理とリカバリーの問題

#### ❌ 問題6: レートリミット発生時のリカバリー戦略が不十分
**場所**: `mobilede_main.js:374-379`

```javascript
if (await isAccessDenied(response)) {
  console.warn(`🚫 アクセス拒否`);
  await detailPage.close();
  await sleep(5000);
  continue; // 同じプロキシでリトライ
}
```

**問題点**:
- **プロキシ変更なし** → 同じプロキシでリトライするだけ
- **ブラウザ状態リセットなし** → コンテキストを閉じない
- **待機時間が固定** → `Retry-After`ヘッダーを無視

#### ❌ 問題7: 連続失敗時の処理が不十分
**場所**: `mobilede_main.js:381-391`

```javascript
if (!hasContent) {
  consecutiveEmpty++;
  if (consecutiveEmpty >= MAX_CONSECUTIVE_EMPTY) {
    await safeCloseContext(); // コンテキストは閉じるが、プロキシ変更なし
  }
}
```

**問題点**:
- **プロキシ変更なし** → コンテキストを閉じるだけで、同じプロキシが再使用される可能性
- **失敗原因の記録なし** → なぜ失敗したかが記録されない

---

### 4. データ収集と分析の問題

#### ❌ 問題8: 失敗モードの詳細記録なし
- **レートリミット発生時のリクエスト数** → 記録されていない
- **レートリミット発生までの時間** → 記録されていない
- **レスポンスヘッダー情報** → 記録されていない
- **プロキシごとの成功率** → 記録されていない

#### ❌ 問題9: テスト用の再現機能なし
- **意図的にレートリミットを発生させる機能がない**
- **リクエスト頻度を調整してテストする機能がない**

---

## 検証すべき質問への回答準備状況

### 質問1: レートリミットはどのタイミングで発生するか？

**現状**: ❌ 検証不可能
- レートリミット発生時の詳細情報が記録されていない
- リクエスト数、時間、プロキシ情報が追跡されていない

**必要な改善**:
1. 各リクエストのタイムスタンプとプロキシ情報を記録
2. レスポンスヘッダー（`Retry-After`, `X-RateLimit-*`）を記録
3. レートリミット発生時のコンテキスト（リクエスト数、経過時間）を記録

### 質問2: プロキシを変えるだけで解決するのか？

**現状**: ❌ 検証不可能
- プロキシ切り替え機能がない
- プロキシ切り替え時のブラウザ状態リセットが不十分
- A/Bテスト（プロキシ変更のみ vs プロキシ+状態リセット）ができない

**必要な改善**:
1. プロキシローテーション機能の実装
2. プロキシ変更時のブラウザ状態リセット機能（明示的なCookie/Storageクリア）
3. テストモード: プロキシ変更のみ vs プロキシ+状態リセットの比較

---

## 推奨される改善アプローチ

### Phase 1: 失敗モードの詳細記録
1. **レスポンス詳細の記録**
   - HTTPステータスコード
   - レスポンスヘッダー（特に `Retry-After`, `X-RateLimit-*`）
   - レスポンスボディ（ブロックページの内容）
   - HTMLコンテンツの検証（「Access Denied」などのキーワード）

2. **リクエストコンテキストの記録**
   - リクエスト数（プロキシごと、時間窓ごと）
   - タイムスタンプ
   - 使用中のプロキシ
   - ブラウザ状態（userDataDirのパス）

### Phase 2: プロキシローテーション機能
1. **プロキシ管理システム**
   - `proxies.json` の読み込み
   - プロキシの健康状態追跡
   - プロキシローテーション機能

2. **プロキシ切り替え時の処理**
   - ブラウザコンテキストの完全なクリーンアップ
   - Cookie/Storageの明示的なクリア
   - userDataDirの削除

### Phase 3: テストと検証機能
1. **レートリミット再現機能**
   - リクエスト頻度を調整する機能
   - 意図的にレートリミットを発生させるテストモード

2. **A/Bテスト機能**
   - プロキシ変更のみ
   - プロキシ変更 + ブラウザ状態リセット
   - 結果の比較と記録

---

## 次のステップ

1. **詳細な失敗ログ機能を実装** → 失敗モードを特定
2. **プロキシローテーション機能を実装** → リカバリーテストが可能に
3. **テストモードを実装** → レートリミット再現とA/Bテスト

